package example.symbolTableTestFiles;

import example.CDs.AuctionCD.*;
import example.CDs.DefaultTypes.*;

ocl test9 {
   context Student st inv Voraussetzung:
    let
      Person p = typeif st instanceof Student then st else Person;
    in
      this;
}