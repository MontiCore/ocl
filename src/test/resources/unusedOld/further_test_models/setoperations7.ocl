/* (c) https://github.com/MontiCore/monticore */
ocl setoperations7 {
  inv:
    let
      Collection<Integer> ci = [1, 2, 1]
in
      result == (typeif ci instanceof List<Integer> then ci.get(0) else -42);
}
