/* (c) https://github.com/MontiCore/monticore */
package invariants;

ocl Test02 {
  inv(int a, int b):
    a + b != 0 &&
    a - b != 0 &&
    a * b != 0 &&
    a / b != 0 &&
    a % b != 0;

  inv(Integer a, Integer b):
    a + b != 0 &&
    a - b != 0 &&
    a * b != 0 &&
    a / b != 0;
}
