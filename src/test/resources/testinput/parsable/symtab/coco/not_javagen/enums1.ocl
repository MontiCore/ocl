/* (c) https://github.com/MontiCore/monticore */
ocl enums1 {
  context Auction a inv Enums1:
    a.status == AuctionStatus.INITIAL || a.status == AuctionStatus.RUNNING || a.status == AuctionStatus.FINISHED;
}
