/* (c) https://github.com/MontiCore/monticore */
ocl invalidParameterType {
  context boolean Person.changeCompany(int gehalt)
  let 
   min = a.startTime.lessThan(a.closingTime)
              ? a.startTime : a.closingTime
  pre: true;
  post: false;
  
}
