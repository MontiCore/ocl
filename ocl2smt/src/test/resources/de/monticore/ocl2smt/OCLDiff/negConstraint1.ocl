/* (c) https://github.com/MontiCore/monticore */
package invariants;
ocl NC1 {
//UNSAT
  inv MaxIdent_9:
        forall Auction a:
           a.auctionIdent <= 9 ;


  //UNSAT
 inv No_Auction_Facebook:
      exists Auction a :
        a.name != "Facebook";
  //UNSAT
 inv Ident_Between_2_And_19:
       forall Auction a:
          (a.auctionIdent >= 0) &&
           (a.auctionIdent < 19);
 //UNSAT
 inv True:
    1==1;
}