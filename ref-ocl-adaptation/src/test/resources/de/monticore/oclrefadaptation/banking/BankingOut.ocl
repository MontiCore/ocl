/* (c) https://github.com/MontiCore/monticore */

ocl bankingChecks {
  context SEPABank b inv OverallBalanceCorrect:
  let
    Set<double> balances = { acc.balance | BankAccount acc in b.bankAccount };
    double calculatedBalance = iterate { balance in balances;
                                  double accumulated = 0.0 :
                                  sum = accumulated + balance }
  in
    b.totalMoney == calculatedBalance;
}